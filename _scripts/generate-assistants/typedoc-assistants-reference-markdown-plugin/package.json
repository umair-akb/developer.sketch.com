{
  "name": "typedoc-assistants-reference-markdown-plugin",
  "version": "1.0.0",
  "description": "A TypeDoc plugin to render Jekyll templates",
  "main": "dist/index.js",
  "keywords": [
    "typedocplugin"
  ],
  "dependencies": {
    "@types/node": "^15.0.3",
    "typedoc": "^0.20.36",
    "typedoc-plugin-markdown": "^3.8.1",
    "typescript": "~4.2.4"
  },
  "engines": {
    "node": ">= 10.8.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc && copyfiles --up 1 ./src/**/*.hbs ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mihai Potra <mihai@sketch.com>",
  "license": "ISC"
}
